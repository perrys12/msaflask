This project is done by the use of Microsoft Azure and Flask. 
The project is a weather app that uses real time data grabbed from OpenWeather API which shows the weather of a location. When a location is typed and Added, it is added in a SQL Database.
